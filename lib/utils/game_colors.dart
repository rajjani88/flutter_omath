import 'package:flutter/material.dart';

class GameColors {
  // Main Background Gradient
  static const Color bgTop = Color(0xFF2E0D46); // Deep Purple
  static const Color bgBottom = Color(0xFF19062D); // Darker Purple

  // Primary Interactive (Yellow/Amber)
  static const Color primary = Color(0xFFFFD700);
  static const Color primaryShadow = Color(0xFFC6A700);

  // Secondary (Teal/Cyan)
  static const Color secondary = Color(0xFF00E5FF);
  static const Color secondaryShadow = Color(0xFF00B0C4);

  // Danger/Wrong (Red/Pink)
  static const Color danger = Color(0xFFFF4081);
  static const Color dangerShadow = Color(0xFFC60055);

  // Success (Green)
  static const Color success = Color(0xFF00E676);
  static const Color successShadow = Color(0xFF00B248);

  // Neutral (White/Grey)
  static const Color surface = Colors.white;
  static const Color textMain = Color(0xFF2E0D46);
  static const Color textLight = Colors.white;

  // Board/Panel Color
  static const Color panel = Color(0xFF4A148C); // Lighter Purple
}
